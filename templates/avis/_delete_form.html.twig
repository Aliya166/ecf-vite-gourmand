<form method="post" action="{{ path('app_avis_delete', {'id': avi.id}) }}" class="js-confirm-form" data-confirm-message="Are you sure you want to delete this item?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avi.id) }}">
    <button class="btn">Delete</button>
</form>
