<form method="post" action="{{ path('app_allergene_delete', {'id': allergene.id}) }}" class="js-confirm-form" data-confirm-message="Etes-vous sur de vouloir supprime cet allergène?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allergene.id) }}">
    <button class="btn">Delete</button>
</form>
