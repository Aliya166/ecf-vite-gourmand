<form method="post" action="{{ path('app_theme_delete', {'id': theme.id}) }}" class="js-confirm-form" data-confirm-message="Are you sure you want to delete this item?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme.id) }}">
    <button class="btn">Delete</button>
</form>
